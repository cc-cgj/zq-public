

	contenteditable 属性指定元素内容是否可编辑。

	<div contenteditable="true|false"></div>

	true:指定元素是可编辑的
	
	false:指定元素是不可编辑的

	注意： 

		1、当元素中没有设置 contenteditable 属性时，元素将从父元素继承。

		2、所有主流浏览器都支持 contenteditable 属性